import SwiftUI
import CoreData
import UniformTypeIdentifiers

// Fake Factura
let ctx = NSManagedObjectContext(concurrencyType: .mainQueueConcurrencyType)

// Compile test_pdf.swift with the rest of the project? No, it's easier to just build a mini project.
